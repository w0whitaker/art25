---
layout: base
schema: BlogPosting
---
<div class="region wrapper">
  <div class="art-image"><img src="{{ image }}" alt="{{ alt }}" eleventy:widths="200,400"></div>
  <div class="art-caption">
    <h1>{{ title }}</h1>
    <p>{{ medium }}</p>
    <p>{{ year }}</p>
  </div>
</div>